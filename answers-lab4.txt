###Question

1. 因为APs在被BSP唤醒时是跑在实模式下，但是BSP已经是保护模式且开启了页模式寻址，所以mpentry.S中要用MPBOOTPHYS来计算物理地址来防止访问错误地址。

2. 因为发生中断时，CPU会直接将一些寄存器值保存到栈上，这时并不会通过软件去检查锁。所以如果多个CPU共享一个栈就有可能导致一个CPU因为中断覆盖了另一个正在使用栈的CPU的数据。

3. 因为在页表切换时，切换的只是用户可见区域的地址映射，而内核区域的地址映射都是相同的，所以页表切换不影响内核地址空间内指针的解引用。
